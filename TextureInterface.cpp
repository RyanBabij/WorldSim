#pragma once
#ifndef GUILD_TEXTURE_INTERFACE_CPP
#define GUILD_TEXTURE_INTERFACE_CPP

/* TextureInterface.cpp
	<>
*/

//USE HASTEXTURE INSTEAD

// class TextureInterface
// {
	// public:
	// /* The object must return the current texture to be displayed when this is called. */
	// //virtual Texture* currentTexture() { DBG return 0; }
	// virtual Texture* currentTexture() =0;
// };

#endif
